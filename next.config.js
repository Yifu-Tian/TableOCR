/** @type {import('next').NextConfig} */
const nextConfig = {}
module.exports = {
  // Use the basePath and assetPrefix settings to adjust the paths for GitHub Pages
  basePath: '/TableOCR',
  assetPrefix: '/TableOCR',

  // Exporting as a static website
  output: 'standalone',
};
module.exports = nextConfig
